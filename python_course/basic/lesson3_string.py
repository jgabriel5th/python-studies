'''
DocString
Python = Programming Language
Typing type = Dynamic / Strong -> Which means Python knows the data type without
without defining it.
str -> string -> text.
Strings are texts inside quotes.
'''

# Single quote
print('Just practing')

# Double quote
print("In order to evolve my logic and English skills")

# Escape \
print("The interpreter will \"read this differently\"")

# r(it'll show the \)
print(r"The interpreter will \"read this differently\"")

# Instead of using Escape
print("It's better to use single quote and double quote instead of 'Escape' in order to avoid a complex code")